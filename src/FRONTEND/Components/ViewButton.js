import React from "react";
import {Link} from "react-router-dom";

const ViewButton = (param) => {
    return (
        <button className="f-button f-view">View</button>
    );
};

export default ViewButton;